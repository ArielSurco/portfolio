<div class="w-full flex justify-center">
  <h2 class="text-4xl font-semibold relative inline-block px-8 py-4 gradient-text">
    <div class="absolute top-0 left-0 w-[30%] h-[50%] border-t-2 border-l-2 border-teal-400"></div>
    <div class="absolute bottom-0 right-0 w-[30%] h-[50%] border-b-2 border-r-2 border-teal-400"></div>
    <slot/>
  </h2>
</div>

<style>
  .gradient-text {
    background: linear-gradient(
      to right,
      #ffffff,
      #99f6e4,
      #ccfbf1,
      #99f6e4,
      #ffffff
    );
    background-size: 200% auto;
    color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
    animation: gradient 3s linear infinite;
    width: fit-content;
    margin: 0 auto;
  }

  @keyframes gradient {
    0% { background-position: 0% center; }
    100% { background-position: 200% center; }
  }
</style>