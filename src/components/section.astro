---
import { cn } from '../utils/cn'


interface Props {
  id?: string
  class?: string
}

const { id, class: className } = Astro.props

---

<section id={id} class={cn('min-h-fit relative text-slate-100 flex flex-col gap-10 justify-center items-center', className)}>
  <slot/>
</section>
